import { Priority, Status, Visibility, Role, EventType, IncidentCategory } from '@prisma/client';

export function Select({name, options, value, label}:{name:string; options:string[]; value?:string|null; label?:string}){return <label className="grid gap-1 text-sm">{label ?? name}<select className="input" name={name} defaultValue={value ?? ''}><option value="">—</option>{options.map(o=><option key={o} value={o}>{o}</option>)}</select></label>}
export function Text({name,label,value,type='text',required=false}:{name:string;label:string;value?:string|null;type?:string;required?:boolean}){return <label className="grid gap-1 text-sm">{label}<input className="input" name={name} type={type} defaultValue={value ?? ''} required={required}/></label>}
export function TextArea({name,label,value}:{name:string;label:string;value?:string|null}){return <label className="grid gap-1 text-sm md:col-span-2">{label}<textarea className="input" name={name} defaultValue={value ?? ''}/></label>}
export const priorities = Object.values(Priority); export const statuses = Object.values(Status); export const visibilities = Object.values(Visibility); export const roles = Object.values(Role); export const eventTypes = Object.values(EventType); export const incidentCategories = Object.values(IncidentCategory);
export function MultiUsers({users, name, selected=[]}:{users:{id:string;name:string}[];name:string;selected?:string[]}){return <label className="grid gap-1 text-sm">Usuarios<select className="input min-h-28" name={name} multiple defaultValue={selected}>{users.map(u=><option key={u.id} value={u.id}>{u.name}</option>)}</select></label>}
export function Filters({areas, users}:{areas:{id:string;name:string}[]; users:{id:string;name:string}[]}){return <form className="card grid gap-3 md:grid-cols-5"><input className="input" name="q" placeholder="Buscar"/><select className="input" name="area"><option value="">Área</option>{areas.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><select className="input" name="status"><option value="">Estado</option>{statuses.map(s=><option key={s}>{s}</option>)}</select><select className="input" name="priority"><option value="">Prioridad</option>{priorities.map(p=><option key={p}>{p}</option>)}</select><select className="input" name="responsible"><option value="">Responsable</option>{users.map(u=><option key={u.id} value={u.id}>{u.name}</option>)}</select><button className="btn md:col-span-5">Filtrar</button></form>}
