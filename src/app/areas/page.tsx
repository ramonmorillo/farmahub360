import { Shell } from '@/components/Shell';import { requireUser } from '@/lib/session';import { canManageUsers } from '@/lib/permissions';import { prisma } from '@/lib/prisma';import { ensureInitialAreasAction, saveArea } from '@/lib/actions';
export default async function Areas(){const user=await requireUser(); const admin=canManageUsers(user); const areas=await prisma.area.findMany({orderBy:{name:'asc'},include:{_count:{select:{users:true,tasks:true,events:true,incidents:true,projects:true}}}}); return <Shell><div className="flex justify-between"><h2 className="text-3xl font-bold">Áreas</h2>{admin&&<form action={ensureInitialAreasAction}><button className="btn">Crear áreas iniciales</button></form>}</div>{admin&&<form action={saveArea} className="card grid gap-3 md:grid-cols-2"><h3 className="font-semibold md:col-span-2">Nueva área</h3><input className="input" name="name" placeholder="Nombre" required/><input className="input" name="description" placeholder="Descripción"/><label className="text-sm"><input type="checkbox" name="active" defaultChecked/> Activa</label><button className="btn">Guardar</button></form>}<div className="grid gap-3 md:grid-cols-2">{areas.map(a=><div className="card" key={a.id}><form action={saveArea} className="grid gap-2"><input type="hidden" name="id" value={a.id}/><input className="input font-semibold" name="name" defaultValue={a.name} disabled={!admin}/><textarea className="input" name="description" defaultValue={a.description??''} disabled={!admin}/><label className="text-sm"><input type="checkbox" name="active" defaultChecked={a.active} disabled={!admin}/> Activa</label>{admin&&<button className="btn">Actualizar</button>}</form><p className="mt-2 text-sm text-slate-500">{a._count.users} usuarios · {a._count.tasks+a._count.events+a._count.incidents+a._count.projects} registros · Sin borrado físico</p></div>)}</div></Shell>}
