import { notFound } from 'next/navigation';
import { requireUser } from '@/lib/session';
import { prisma } from '@/lib/prisma';
import { secureWhere } from '@/lib/data';
import { formatDate, labelPriority, labelStatus } from '@/lib/print';
export default async function IncidentPrint({params}:{params:{id:string}}){const user=await requireUser(); const incident:any=await prisma.incident.findFirst({where:{AND:[{id:params.id},secureWhere(user,false,false) as any]},include:{area:true,responsible:true,comments:{include:{author:true},orderBy:{createdAt:'desc'}}}}); if(!incident) notFound(); const audits=await prisma.auditLog.findMany({where:{entityType:'INCIDENT',entityId:incident.id},include:{user:true},orderBy:{createdAt:'desc'}}); return <main className="mx-auto max-w-4xl bg-white p-8 print:p-0"><button className="btn mb-6 print:hidden">Imprimir / Guardar como PDF</button><script dangerouslySetInnerHTML={{__html:"document.querySelector('button')?.addEventListener('click',()=>window.print())"}}/><header className="border-b pb-4"><h1 className="text-3xl font-bold">FarmaHub360</h1><p>Gestión interna del Servicio de Farmacia</p><p className="mt-2 text-xl font-semibold">Informe de incidencia</p><p>Fecha de generación: {formatDate(new Date(),true)}</p></header><section className="mt-6 grid gap-3"><h2 className="text-2xl font-bold">{incident.title}</h2><p><b>Descripción:</b> {incident.description??'—'}</p><p><b>Categoría:</b> {incident.category}</p><p><b>Área afectada:</b> {incident.area?.name??'—'}</p><p><b>Responsable:</b> {incident.responsible?.name??'—'}</p><p><b>Estado:</b> {labelStatus(incident.status)}</p><p><b>Prioridad:</b> {labelPriority(incident.priority)}</p><p><b>Fecha detección:</b> {formatDate(incident.detectedAt,true)}</p><p><b>Fecha resolución:</b> {formatDate(incident.resolvedAt,true)}</p><p><b>Acciones realizadas:</b> {incident.actions??'—'}</p></section><section className="mt-6"><h3 className="text-xl font-bold">Comentarios</h3>{incident.comments.length?incident.comments.map((c:any)=><p className="mt-2" key={c.id}>{formatDate(c.createdAt,true)} · {c.author.name}: {c.text}</p>):<p>—</p>}</section><section className="mt-6"><h3 className="text-xl font-bold">Historial de cambios</h3>{audits.length?audits.map(a=><p className="mt-2" key={a.id}>{formatDate(a.createdAt,true)} · {a.user?.name??'Sistema'} · {a.action} · {a.summary}</p>):<p>—</p>}</section><footer className="mt-10 border-t pt-4 text-sm"><p>FarmaHub360 · Desarrollado por Ramón Morillo · 2026</p><p>Documento de gestión interna. La identificación del paciente, si procede, debe realizarse en el sistema corporativo autorizado.</p></footer></main>}
