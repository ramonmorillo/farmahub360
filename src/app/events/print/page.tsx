import { requireUser } from '@/lib/session';import { prisma } from '@/lib/prisma';import { secureWhere } from '@/lib/data';import { formatDate,labelPriority,labelVisibility,taskFilters } from '@/lib/print';
export default async function EventsPrint({searchParams}:{searchParams:Record<string,string|undefined>}){const user=await requireUser();const rows=await prisma.event.findMany({where:{AND:[secureWhere(user,true,false),taskFilters(searchParams)]} as any,include:{area:true,responsible:true},orderBy:{startAt:'asc'},take:500});return <main className="mx-auto max-w-6xl bg-white p-8 print:p-0"><button className="btn mb-6 print:hidden">Imprimir / Guardar como PDF</button><script dangerouslySetInnerHTML={{__html:"document.querySelector('button')?.addEventListener('click',()=>window.print())"}}/><h1 className="text-3xl font-bold">FarmaHub360</h1><p>Gestión interna del Servicio de Farmacia</p><h2 className="mt-2 text-xl font-semibold">Listado filtrado de eventos</h2><p>Fecha de generación: {formatDate(new Date(),true)}</p>{rows.length?<table className="mt-6 w-full text-sm"><tbody>{rows.map(r=><tr key={r.id}><td>{r.title}</td><td>{r.area?.name??'—'}</td><td>{r.responsible?.name??'—'}</td><td>{formatDate(r.startAt,true)}</td><td>{formatDate(r.endAt,true)}</td><td>{labelPriority(r.priority)}</td><td>{labelVisibility(r.visibility)}</td></tr>)}</tbody></table>:<p className="mt-8">No existen registros para los filtros seleccionados.</p>}</main>}
